<h1>Export</h1>
<div id="wizard-start" class="wizard-card">
    <span>Select output format(s):</span>
    <div class="export-wizard-formats">
        {{#each template.export_formats}}
        <div class="form-check">
            <input class="form-check-input export-wizard-format" type="checkbox" value="{{slug}}" id="export-wizard-format-{{slug}}">
            <label class="form-check-label" for="export-wizard-format-{{slug}}">
                {{name}}
            </label>
        </div>
        {{/each}}
    </div>
    <span>What do you want to export?</span>
    <div class="form-check">
        <input class="form-check-input" type="radio" name="export-wizard-export-select" id="export-wizard-select-all" checked>
        <label class="form-check-label" for="export-wizard-select-all">
            The whole Project
        </label>
    </div>
    <div class="form-check">
        <input class="form-check-input" type="radio" name="export-wizard-export-select" id="export-wizard-select-selected-chapters">
        <label class="form-check-label" for="export-wizard-select-selected-chapters">
            Only selected chapters
        </label>
    </div>
    <div id="export-wizard-selected-chapters" class="ms-4 hide">
        {{#each sections}}
        {{#if Section}}
        <div class="form-check">
            <input class="form-check-input export-wizard-selected-chapter" type="checkbox" value="{{Section.id}}" id="export-wizard-selected-chapter-{{Section.id}}">
            <label class="form-check-label" for="export-wizard-selected-chapter-{{Section.id}}">
                {{Section.metadata.title}}
            </label>
        </div>
        {{/if}}
        {{/each}}
    </div>
    <div class="overlay-bottom-row">
        <button class="btn btn-primary float-end" id="export-wizard-start-export">Start Export</button>
    </div>
</div>
<div id="wizard-working" class="wizard-card hide">
    <!-- Loading spinner -->
    <div class="d-flex align-items-center justify-content-center mt-3">
        <div class="loader" id="wizard-working-loader"></div>
    </div>
    <!-- Current task -->
    <small style="text-align: center" id="wizard-working-current-step"></small>
</div>
<div id="wizard-download" class="wizard-card hide">
    <span>Done!</span>
    <div class="d-flex align-items-center justify-content-center mt-3">
        <a href="" download id="wizard-download-link"><button class="btn btn-primary" id="wizard-download-btn">Download Files</button></a>
    </div>
</div>
<div id="wizard-show-error" class="wizard-card hide">
    <span>Error occurred :(</span>
    <span class="mt-2">Log:</span>
    <div id="wizard-show-error-log-pane">

    </div>
</div>